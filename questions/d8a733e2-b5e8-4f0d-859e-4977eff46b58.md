# 问题

Agent 的短期记忆和长期记忆有什么区别？

# 回答

短期记忆是一次会话或任务线程内的状态，如历史消息、已完成步骤、工具结果和待审批动作。它通常随 thread/run 隔离，可以通过检查点在同一任务中恢复。

长期记忆跨会话存在，如经用户确认的偏好、稳定业务事实、过往任务结果或可复用经验。它需要独立的身份和命名空间、写入策略、检索策略、更新/删除机制与保留期。

两者都不等于当前 prompt：持久化状态只有在被选择、裁剪并投影进当次模型请求后才占用上下文。也不应把所有对话原文无期限当作长期记忆。

# 面试考察点

能否区分存储范围、生命周期和当次上下文投影。
